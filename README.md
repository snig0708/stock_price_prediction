This project builds a deep learning pipeline to forecast stock prices using RNN, LSTM, and GRU in PyTorch.

The objective is to predict the next day’s closing price and compare model performance against a naive baseline.